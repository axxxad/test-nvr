DATABASE_URL=sqlite:////data/nvr.db
RECORDINGS_DIR=/recordings
SECRET_KEY=change-me-in-production
# IANA zone — must match TZ in docker-compose (e.g. Europe/Amsterdam, Europe/London).
APP_TIMEZONE=Europe/London
TZ=Europe/London
